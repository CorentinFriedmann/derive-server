{
  "name": "derive-server",
  "version": "1.0.0",
  "private": true,
  "description": "Serveur Dérive : proxy sécurisé vers l'API Claude + persistance des séjours et de l'historique.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
